PROMPT TO SEND TO CHATGPT:

I need you to generate UML diagrams for my EduPredict academic performance prediction system. Please create the following diagrams using PlantUML syntax:

1. CONTEXT DIAGRAM - Show the EduPredict system with actors (Admin, Instructor, Student) and external systems (MySQL Database)

2. USE CASE DIAGRAM - Show all use cases organized into packages:
   - Authentication (Login, Register, Logout)
   - Dashboard (Role-specific dashboards for Admin, Instructor, Student)
   - Student Management (View, Add, Update, Delete, View Profile)
   - Course Management (View, Add, Update, Delete, View Details)
   - Grade Management (View, Add, Update, View History)
   - Prediction System (Predict Student Performance, View Predictions, Predict Course Performance, View History)
   - Alert System (View Alerts, Create Alert, Resolve Alert, Dismiss Alert)
   - User Management (View, Add, Update, Delete Users)

3. SEQUENCE DIAGRAMS - Create 3 sequence diagrams:
   a) Predict Student Performance - Show flow from user request through Router → PredictionController → PredictionService → KNNPredictor → Models → Database
   b) User Login - Show authentication flow from login form through Router → AuthController → UserModel → Database → Session
   c) Alert Creation - Show how alerts are created when PredictionService detects high-risk students

4. CLASS DIAGRAM - Show all classes organized into packages:
   - Core: Database (Singleton), Router, Controller (base), Model (base), Validator
   - Controllers: AuthController, PredictionController, AlertController, DashboardController, HomeController, PageController, CrudController
   - Models: UserModel, StudentModel, CourseModel, GradeModel, AlertModel, PredictionModel
   - Services: PredictionService
   - ML: KNNPredictor
   Show inheritance relationships (Controllers extend Controller, Models extend Model) and dependencies

5. STATE DIAGRAMS - Create 2 state diagrams:
   a) Student Risk Level Management - States: Initial → DataCollection → Prediction → LowRisk/MediumRisk/HighRisk → AlertCreated → Intervention → Monitoring → Escalation
   b) User Session Management - States: LoggedOut → Authenticating → LoggedIn (with role-based dashboards) → SessionExpired → LoggedOut

System Details:
- MVC architecture (PHP)
- Uses KNN (K-Nearest Neighbors) machine learning algorithm for predictions
- Role-based access: Admin, Instructor, Student
- Database: MySQL (edupredict)
- Main features: Student performance prediction, grade management, alert system, course management

Please provide the complete PlantUML code for all diagrams that I can use to generate visual diagrams.






